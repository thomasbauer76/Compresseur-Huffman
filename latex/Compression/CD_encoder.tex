\begin{algorithme}
 \procedure{encoder}{\paramEntreeSortie{fbInitial : \fichierbinaire, fbCompresse : \fichierbinaire}, \paramEntree{tdc : \tabledecodage}}
    {estOuvert(fb1) et (mode(fb1) $=$ lecture) et estOuvert(fb2) et (mode(fb2) $=$ écriture)}
    {cbTemp, cb: \codebinaire, o : \octet, i : \naturel}
    {
		\instruction{positionnerAuDebut(f)}
		\\
        \affecter{cbTemp}{creerCodeBinaire(bitA0)}
        \pour{i}{ 1}{7}{}{\instruction{ajouterBit(cbTemp,bitA0)}}
        \\
        \tantque{$non$ finfichier(fnInitiale)}{
       		
        		\instruction{lireoctet(fbInitiale, o)}
        		\affecter{cb}{octetVersCodeBinaire(tdc, o)}
        	 	\instruction{concatenerCodeBinaireDansFichier(cbTemp, cb, fbcompresse)}
        		}
        \\
        \sialors{obtenirLongueur(cbTemp) $<$ 8}
		{
			\pour{i}{ obtenirLongeur(cbTemp)}{7}{}
        			{
        					\instruction{ajouterBit(cbTemp, bitA0)}
        			}
        	\instruction{ecrireOctet(fbCompresse, codeBinaireEnOctet(cbTemp))}
		}
    }
\end{algorithme}
    	    	